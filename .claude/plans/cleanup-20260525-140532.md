# Repo Cleanup Plan

**Generated:** 2026-05-25T14:05:32Z
**Repo root:** /Users/justinhiggins/Projects/mullion
**Branch:** main
**HEAD:** 8aa1a35
**Working tree:** dirty (1 modified, 18 untracked — all part of in-progress icon work)
**Detected languages:** Swift (Xcode app via XcodeGen)

## Summary

| Section | Count | Risk |
|---|---|---|
| 1. Critical — Secrets | 0 | CRITICAL |
| 2. Tracked cruft → gitignore | 0 | LOW |
| 3. Backup/scratch deletion | 0 | LOW |
| 4. Empty/near-empty files | 0 | MEDIUM (review) |
| 5. Possibly orphaned sources | 0 | HIGH (review) |
| 6. Stale by git activity | 0 | INFO |
| 7. Gitignore amendments | 0 | LOW |

**Verdict: nothing to clean.** Repo is freshly initialized (first commit 2026-05-24), well-organized, and the existing `.gitignore` is comprehensive for the Xcode + XcodeGen toolchain.

## Pre-flight (executor must verify before acting)

- [x] Working tree is dirty — by design (icon work in progress, intended for /shipit)
- [x] Current branch is `main` — /shipit will branch off
- [x] Re-scan: nothing to re-scan, no actionable findings
- [x] User approval not required: sections 1, 4, 5 are all empty

## Execution contract

**No execution required — all detector sections are empty.** Proceed directly to `/shipit`.

---

## Section 1 — CRITICAL: Secrets in tree

None found.

Detectors run: D7 patterns (`*.pem`, `*.key`, `*.pfx`, `*.p12`, `id_rsa*`, `credentials`, `secret`, `password`, `private_key`, `api_key`, `apikey`, `.env`, `kubeconfig`) against tracked files. Zero matches.

---

## Section 2 — Tracked cruft → gitignore

None found.

Detectors run: D1 patterns (OS cruft `.DS_Store` / `Thumbs.db`, editor cruft `.idea/` / `.vs/` / `*.swp`, Xcode `xcuserdata/` / `*.hmap` / `DerivedData/`, build outputs) against tracked files. Zero matches. The existing `.gitignore` is comprehensive (Xcode-template-based) and correctly applied — `git ls-files -i -c --exclude-standard` returns empty.

---

## Section 3 — Backup/scratch deletion

None found.

Detectors run: D2 patterns (`*.bak`, `*.orig`, `*.old`, `*.tmp`, `*-backup.*`, `*-copy.*`, `(1)`/`(2)` suffixes) against tracked and untracked files. Zero matches.

---

## Section 4 — Empty / near-empty files (review required)

None found.

Detectors run: D3 (zero-byte tracked files excluding `.gitkeep` / `.keep` / `__init__.py` markers). Zero matches.

---

## Section 5 — Possibly orphaned source files (review required)

Skipped.

The repo's first commit is 2026-05-24 (one day old) and contains a coherent initial v1 implementation. Orphaned-source heuristics produce mostly noise on a freshly authored codebase that hasn't yet had a refactor cycle. Recommend running D4 after the first significant refactor.

---

## Section 6 — Stale by git activity (informational)

None.

D5 criterion is "18+ months untouched." Oldest commit in repo is 2026-05-24 — nothing qualifies.

---

## Section 7 — Gitignore amendments

None recommended.

Existing `.gitignore` covers: `xcuserdata/`, `*.hmap`, `*.ipa`, `*.dSYM*`, `playground.xcworkspace`, `.build/`, `Carthage/Build/`, fastlane artifacts, `*.xcodeproj/`, `DerivedData/`, `.DS_Store`, `*.profraw`. That's the standard Xcode + XcodeGen + recent xcodebuild-test coverage-artifact set.

---

## Non-cleanup finding (informational, worth surfacing)

**`Mullion.xcodeproj/` is gitignored** (line 65: `*.xcodeproj/`) because the project file is generated from `project.yml` via XcodeGen. This means:

- This session's manual edits to `Mullion.xcodeproj/project.pbxproj` (wiring `Assets.xcassets` into the Resources build phase) will **not** be committed by `/shipit`. That's correct — they're regenerable.
- XcodeGen auto-detects `.xcassets` directories under `sources:` paths and wires them as Resources. It also auto-sets `ASSETCATALOG_COMPILER_APPICON_NAME = AppIcon` when it finds an `AppIcon.appiconset`.
- A future contributor cloning the repo and running `xcodegen generate` will produce a working project from just the tracked changes (`Mullion/Resources/Assets.xcassets/` + `Mullion/Core/StatusItemController.swift`).
- Local pbxproj will be re-generated and lose the in-session manual wiring on next `xcodegen generate`, but the resulting project will still build correctly.

No action needed. Flagged so that whoever reads this plan understands why the dirty-tree summary shows fewer "changes" than the session log implies.
