When writing Swift: target Swift 6 / iOS 17+. Use @Observable not ObservableObject.
   Use NavigationStack not NavigationView. Use foregroundStyle not foregroundColor.
   No force unwraps. Mark types Sendable where crossing actor boundaries.